# ── UI ──
streamlit>=1.36.0,<2.0.0

# ── Scraping acb.com + cliente LLM (OpenAI-compatible vía httpx) ──
httpx>=0.27.0,<0.30.0
beautifulsoup4>=4.12.0,<5.0.0

# ── Generación de PDF ──
weasyprint>=62.0,<70.0
